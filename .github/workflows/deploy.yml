name: Deploy

on:
  push:
    branches:
      - main
      - "**"

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: false

jobs:
  check:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Build & Lint & Test backend
            command: ./packages/backend/scripts/build-types.ts && ./packages/backend/scripts/lint.ts && cd packages/backend && npx tsx --test src/**/__tests__/**/*.test.ts
          - name: Build & Lint & Test frontend
            command: ./packages/backend/scripts/build-types.ts && ./packages/frontend/scripts/build-types.ts && ./packages/frontend/scripts/lint.ts && cd packages/frontend && ../../scripts/with-env.sh --env=test npx vitest run
    name: ${{ matrix.name }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version-file: ".nvmrc"
          cache: npm
      - uses: actions/cache@v5
        id: nm-cache
        with:
          path: node_modules
          key: node-modules-${{ hashFiles('package-lock.json') }}
      - if: steps.nm-cache.outputs.cache-hit != 'true'
        run: npm ci
      - run: ${{ matrix.command }}

  deploy:
    needs: check
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version-file: ".nvmrc"
          cache: npm
      - uses: actions/cache@v5
        id: nm-cache
        with:
          path: node_modules
          key: node-modules-${{ hashFiles('package-lock.json') }}
      - if: steps.nm-cache.outputs.cache-hit != 'true'
        run: npm ci

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: us-east-1

      - run: ./packages/backend/scripts/pack-build.ts
      - uses: actions/cache@v5
        id: deploy-deps
        with:
          path: packages/backend/dist/node_modules
          key: backend-deploy-deps-${{ hashFiles('packages/backend/package.json') }}
      - if: steps.deploy-deps.outputs.cache-hit != 'true'
        run: ./packages/backend/scripts/pack-install.ts
      - name: Deploy backend
        run: ./packages/backend/scripts/deploy.ts --name=${{ github.ref_name }}

      - name: Deploy frontend
        env:
          # packages/frontend/src/env.d.ts
          POSTHOG_KEY: "phc_i137na3J3eT0uJCvdNrfpywJ1Tt3bXuopRLIwoJsqU2"
          POSTHOG_HOST: "https://us.i.posthog.com"
        run: ./packages/frontend/scripts/deploy.ts --name=${{ github.ref_name }}

  # notify:
  #   needs: [check, deploy]
  #   if: always() && (failure() || (success() && github.ref == 'refs/heads/main'))
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Notify Telegram
  #       run: |
  #         STATUS="${{ needs.deploy.result == 'success' && '✅ Deploy Success' || '❌ CI Failed' }}"
  #         curl -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
  #           -H "Content-Type: application/json" \
  #           -d '{
  #             "chat_id": 647533447,
  #             "parse_mode": "Markdown",
  #             "text": "*'"$STATUS"'*\n\nBranch: `${{ github.ref_name }}`\nCommit: `${{ github.sha }}`\n\n[View Run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})"
  #           }'
